cmake_minimum_required(VERSION 3.10)

# Set the project name
project(proxy)

# Add source files
set(SOURCE_FILES main.cpp)

# Create the executable
add_executable(${CMAKE_PROJECT_NAME} ${SOURCE_FILESm})
